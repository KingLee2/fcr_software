collision_monitor:
  ros__parameters:
    base_frame_id: "$(var mvibot_seri)/base_link" # "mvibot/base_footprint"
    odom_frame_id: "$(var mvibot_seri)/odom"
    cmd_vel_in_topic: "/$(var mvibot_seri)/cmd_vel_smoothed"
    cmd_vel_out_topic: "/$(var mvibot_seri)/cmd_vel"
    state_topic: "/$(var mvibot_seri)/collision_monitor_state"
    transform_tolerance: 0.2
    source_timeout: 1.0
    base_shift_correction: True
    stop_pub_timeout: 2.0
    # Polygons represent zone around the robot for "stop", "slowdown" and "limit" action types,
    # and robot footprint for "approach" action type.
    polygons: ["FootprintApproach"]
    FootprintApproach:
      type: "polygon"
      action_type: "approach"
      footprint_topic: "/$(var mvibot_seri)/local_costmap/published_footprint"
      time_before_collision: 1.2
      simulation_time_step: 0.1
      min_points: 6
      visualize: False
      enabled: True
    observation_sources: ["scan"]
    scan:
      type: "scan"
      topic: "/$(var mvibot_seri)/laser/scan_m5" #"scan"
      min_height: 0.15
      max_height: 2.0
      enabled: True